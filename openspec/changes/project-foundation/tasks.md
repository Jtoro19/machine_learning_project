# Tasks: Project Foundation

Five phases, one per commit slice, matching the design's layered dependency chain (each slice imports
only from earlier slices, so reverse-order revert is always safe). Each phase ends with a mandatory
review checkpoint: stop, report, and wait for the user's explicit authorization before committing and
before starting the next phase. No commit is authorized yet — this file is reviewed before Slice 1
begins.

Total: 52 tasks across 5 phases (11 + 10 + 13 + 9 + 9, including one review checkpoint per phase),
plus a 9-item cross-cutting acceptance checklist verified at the review checkpoints.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~1,650 (additions + deletions, excluding `uv.lock` and generated `results/` output) |
| 400-line budget risk | High |
| Chained PRs recommended | No |
| Suggested split | Five sequential commits on `main`, not pull requests (user-resolved) |
| Delivery strategy | ask-on-risk |
| Chain strategy | stacked-to-main (semantic analog only — slices land on `main` in commit order, exactly like stacked PRs, but as direct commits with no PR wrapper) |

```text
Decision needed before apply: No
Chained PRs recommended: No
Chain strategy: stacked-to-main
400-line budget risk: High
```

The `ask-on-risk` question this forecast would normally trigger was already asked and answered by the
user at session start: **five sequential commits on `main`, not pull requests.** The user reviews and
authorizes each slice before the next begins; an external reviewer (GGA) reviews each commit. No
further delivery decision is needed before `sdd-apply` starts Slice 1.

### Suggested Work Units

| Unit | Goal | Est. lines | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|----------------------|-----------------|-------------------|
| 1 — Packaging | Installable `nids` package skeleton: paths, columns, empty dirs | ~300 | `uv run pytest tests/test_paths.py tests/test_columns.py tests/test_repo_hygiene.py -v` | `uv sync && uv run python -c "import nids"` | `git revert` the packaging commit; nothing else exists yet to depend on it |
| 2 — Data access | Read-only raw CSV loading + validation script | ~250 | `uv run pytest tests/test_loading.py tests/test_validation.py -v` | `uv run python -m nids.validation` (with and without `data/raw/` present) | `git revert` the data-access commit; Slice 1 still stands alone |
| 3 — Cleaning | Six ordered unfitted operations, all diagnostics, `load_clean_partitions` | ~360 | `uv run pytest tests/test_cleaning.py tests/test_data.py tests/test_leak_safety.py tests/test_repo_hygiene.py -v` | `uv run python -c "from nids.data import load_clean_partitions; load_clean_partitions()"` (requires `data/raw/`) | `git revert` the cleaning commit; Slices 1–2 still stand alone |
| 4 — Preprocessing | `RareCategoryGrouper`, pipeline factory, stratified subsample | ~380 | `uv run pytest tests/test_transformers.py tests/test_preprocessing.py tests/test_sampling.py tests/test_leak_safety.py tests/test_repo_hygiene.py -v` | `uv run python -c "from nids.preprocessing import build_preprocessor; build_preprocessor()"` | `git revert` the preprocessing commit; Slices 1–3 still stand alone |
| 5 — Results | Output-contract writer, cleaning report, README completion | ~340 | `uv run pytest tests/test_results.py tests/test_cleaning_report.py tests/test_repo_hygiene.py -v` | `uv run python -m nids.cleaning_report` (requires `data/raw/`) | `git revert` the results commit; Slices 1–4 still stand alone |

---

## Phase 1: Packaging (Slice 1)

**Delivers**: an installable `nids` package with repository-root resolution and the frozen column
allow-lists, plus the empty repository scaffolding. **Reviewable against**: `uv sync` then
`uv run python -c "import nids"` succeeds with no `data/raw/` needed; `repo_root()` resolves correctly
and raises loudly outside a repository; `feature_columns()` returns 39/37 names excluding both
targets; the three feature-routing groups (categorical/skewed/numeric) are disjoint and exhaustive.

- [x] 1.1 Add `[build-system]` (`requires = ["hatchling"]`, `build-backend = "hatchling.build"`), `[tool.hatch.build.targets.wheel] packages = ["src/nids"]`, and `[tool.pytest.ini_options]` (`testpaths = ["tests"]`, `markers = ["slow: ..."]`, `addopts = ["-m", "not slow"]`) to `pyproject.toml`. No other table changes; dependencies stay as declared. Spec: project-packaging — "Installable src-layout package" (both scenarios). Verify: `uv sync` completes with no error.
- [x] 1.2 Create `src/nids/__init__.py` with the package docstring and `__all__: list[str] = []`; no re-export of anything importing pandas, scikit-learn, or matplotlib (Decision 11). Spec: project-packaging — supports "Installable src-layout package". Verify: `uv run python -c "import nids, sys; assert nids.__all__ == []; assert 'pandas' not in sys.modules"`.
- [x] 1.3 Create `src/nids/paths.py`: `REPO_MARKERS`, `REPO_ROOT_ENV_VAR`, `NOTEBOOK_ID_PATTERN`, `RepoRootNotFoundError`, `find_repo_root(start)`, `repo_root()`, `raw_data_dir()` (no `create` parameter — never writes under `data/raw/` — read-only), `results_root()`, `results_dir(notebook_id, *, create=True)` (validates `notebook_id` against the pattern before any `mkdir`, re-checks `resolved.is_relative_to(results_root().resolve())` after resolution). Follow design Decision 1 exactly: `__file__`-based upward walk, then `Path.cwd()` fallback, then `NIDS_REPO_ROOT` validated against the same two markers, no caching. Spec: project-packaging — "Repository-root resolution with no hardcoded absolute paths" (both scenarios). Verify: unit tests in 1.8.
- [x] 1.4 Create `src/nids/columns.py`: `LeakageKey` literal type, `RAW_COLUMNS` (45), `DROPPED_COLUMNS` (`id`, `stcpb`, `dtcpb`, `is_ftp_login`), `TARGET_COLUMNS` (`attack_cat`, `label`), `FEATURE_COLUMNS` (39, explicit allow-list), `KEPT_COLUMNS` (41), `CATEGORICAL_COLUMNS`, `RARE_GROUPED_COLUMNS = ("proto", "service", "state")` (**D1 resolved — `service` is rare-grouped**), `SKEWED_COLUMNS` (the frozen 15-name list per design's concrete partition table), `TTL_SHORTCUT_COLUMNS` (`sttl`, `ct_state_ttl`), `SERVICE_DASH`, `SERVICE_NONE`, `OTHER_CATEGORY`, `RARE_CATEGORY_THRESHOLD = 0.01`, `feature_columns(include_ttl=True)`, `leakage_key_columns(key="full_row")` (**takes no TTL parameter, ever**), `numeric_feature_columns(include_ttl=True)`, `skewed_feature_columns(include_ttl=True)`. Every column name is written in this module and nowhere else. Spec: data-cleaning — restates AGENTS.md (id dropped, `attack_cat`/`label` never features, rare-`proto` grouping groundwork); preprocessing-pipeline — "The feature set is an explicit allow-list" (both scenarios). Verify: unit tests in 1.9.
- [x] 1.5 Create empty tracked directories: `notebooks/.gitkeep`, `skills/.gitkeep`, `dashboard/.gitkeep`, `results/.gitkeep`. Spec: proposal scope deliverable 1; results-output-contract — groundwork for "results/ is committed". Verify: `git status` shows the four files as new/tracked once staged.
- [x] 1.6 Create `README.md` skeleton: title, one-paragraph project summary, a `## Setup` section with `uv sync`, and placeholder headings for `## Data acquisition`, `## Running notebooks`, `## Validation and reports`, `## Known limitations` (filled in Phase 5). Spec: proposal deliverable 8 (partial). Verify: manual read-through; no absolute paths present.
- [x] 1.7 Create `tests/test_paths.py`: marker search succeeds from a nested temp tree and fails loudly (raises `RepoRootNotFoundError` naming both markers and both tried start directories) outside one; `NIDS_REPO_ROOT` is honored only when it carries both markers, ignored otherwise; `results_dir` rejects `../x`, a leading `/`, and `Bad-Id`, and creates `tables/` and `figures/` on success; `raw_data_dir()` has no `create` parameter and never creates its target. Use `tmp_path` fake repos, `monkeypatch.chdir`, `monkeypatch.setenv`. Spec: project-packaging — "Repository root resolves independent of the calling process working directory" and "No absolute path literals exist in the package source" scenarios. Verify: `uv run pytest tests/test_paths.py -v`.
- [x] 1.8 Create `tests/test_columns.py`: `len(RAW_COLUMNS) == 45`, post-drop count is 41, `feature_columns(True)` has 39 names and `feature_columns(False)` has 37; neither `attack_cat` nor `label` appears in `feature_columns()` under either toggle; `TTL_SHORTCUT_COLUMNS` are present in `leakage_key_columns()` under every call (confirming the toggle never applies there); the categorical/skewed/numeric routing groups are pairwise disjoint and their union equals `FEATURE_COLUMNS`; every `SKEWED_COLUMNS` member is non-negative on a synthetic fixture. Spec: preprocessing-pipeline — "The feature set is an explicit allow-list" (both scenarios); data-cleaning restated AGENTS.md rules. Verify: `uv run pytest tests/test_columns.py -v`.
- [x] 1.9 Create `tests/test_repo_hygiene.py` with the AST-walk harness used by every later slice: parse every module under `src/nids/**` and `tests/**`, flag any integer literal `>= 10_000` not on a small, explicitly maintained allow-list (empty at this point — no module has such a literal yet). This is the mechanism Phases 2–5 extend; it must pass cleanly now with zero violations. Hard constraint groundwork: no hardcoded cleaned-partition counts anywhere in `src/nids` or `tests`. Verify: `uv run pytest tests/test_repo_hygiene.py -v`.
- [x] 1.10 Confirm every function added in this phase (`paths.py`, `columns.py`) has type hints, a docstring (summary, `Args`, `Returns`, `Raises` as applicable), and pytest coverage per AGENTS.md. Verify: manual review pass + `uv run pytest tests/test_paths.py tests/test_columns.py -v` all green.
- [ ] 1.11 **Review checkpoint.** Stop. Report the Slice 1 diff summary (files changed, line counts against the ~300-line estimate) and the full test output for `uv run pytest tests/test_paths.py tests/test_columns.py tests/test_repo_hygiene.py -v`. Wait for the user's explicit authorization before committing `feat(packaging): add build system and package skeleton` and before starting Phase 2.

---

## Phase 2: Data access (Slice 2)

**Delivers**: deterministic, read-only loading of both raw partitions and a validation script with
manual-download guidance. **Reviewable against**: `uv run python -m nids.validation` prints a green
report when `data/raw/` is present and download instructions with exit code 1 when it is absent; both
partitions parse to identical dtypes; `"-"` survives parsing as a literal string.

- [ ] 2.1 Create `src/nids/loading.py`: `RAW_TRAIN_FILENAME`, `RAW_TEST_FILENAME`, `STRING_DTYPE = "str"`, `STRING_COLUMNS = ("proto", "service", "state", "attack_cat")`, `RawSchemaError`, `raw_data_present(raw_dir=None)`, `load_raw_train(raw_dir=None)`, `load_raw_test(raw_dir=None)`, `_read_partition(path)` (single parse implementation both loaders go through; `pd.read_csv(path, dtype={c: STRING_DTYPE for c in STRING_COLUMNS})`; raises `RawSchemaError` naming the file and the missing/unexpected columns when `tuple(frame.columns) != RAW_COLUMNS`; `"-"` is not added to `na_values`). Reads only from `data/raw/` (read-only) — no write, rename, or mutation. Spec: data-loading — "Read-only loading of both raw partitions" (both scenarios). Verify: unit tests in 2.4.
- [ ] 2.2 Create `src/nids/validation.py`: `PartitionExpectation`, `RAW_EXPECTATIONS = (PartitionExpectation(RAW_TRAIN_FILENAME, 175_341, 45), PartitionExpectation(RAW_TEST_FILENAME, 82_332, 45))` (**the only place `175_341`, `82_332`, and `45` may appear as literals anywhere in `src/nids`**), `PartitionCheck` (with `ok` property), `ValidationReport` (with `ok` property and `render()`), `validate_raw_data(raw_dir=None)` (checks presence, shape, column names, numeric dtypes, null count — read-only from `data/raw/`), `download_instructions(raw_dir=None)` (names both expected filenames), `main(argv=None) -> int` (prints the report; on failure also prints download instructions; never downloads anything; returns 0 or 1). Spec: data-loading — "Raw data validation with manual-download guidance" (all 3 scenarios) and "No automated dataset acquisition anywhere in the package" scenario. Verify: unit tests in 2.5.
- [ ] 2.3 Create `tests/conftest.py` fixture architecture: `row_template` (session-scoped, one valid 45-column row with neutral defaults), `row_factory` (builds a full 45-column dict from keyword overrides), `frame_factory` (builds a `DataFrame` with columns in `RAW_COLUMNS` order using the same dtype map as `loading._read_partition`), `tmp_results_root`, `frozen_clock`. Spec: supports data-loading test coverage; groundwork for later slices. Verify: `uv run pytest --fixtures | grep -E "row_template|row_factory|frame_factory|tmp_results_root|frozen_clock"` lists all five.
- [ ] 2.4 Create `tests/test_loading.py`: both partitions parse to identical dtypes on `tmp_path`-written CSVs; `"-"` survives as the literal two-character string; `RawSchemaError` raised on a reordered or missing column, naming the offending file; `raw_data_present` correctly reports true/false on a temp directory; loading never modifies the source files on disk (byte comparison before/after each loader call). Spec: data-loading — "Both raw partitions load with stable dtypes" and "Raw files are never modified by loading" scenarios. Verify: `uv run pytest tests/test_loading.py -v`.
- [ ] 2.5 Create `tests/test_validation.py`: missing file → `ok is False`, instructions printed via `capsys`, `main() == 1`; wrong shape reported per-partition; `main() == 0` on a synthetic well-formed pair written to `tmp_path`; validation failure triggers no network call, no subprocess, and no download attempt (assert absence via `monkeypatch` guard or AST inspection of the module). Spec: data-loading — "A required raw file is missing", "A present raw file does not match the expected raw shape", "Validation failure never triggers acquisition" scenarios. Verify: `uv run pytest tests/test_validation.py -v`.
- [ ] 2.6 Add real-file tests to `tests/test_validation.py`: `test_raw_partitions_match_expected_shape` and `test_raw_columns_match_allow_list`, asserting 175,341×45 and 82,332×45 against the actual `data/raw/` (read-only) CSVs, each guarded by `@pytest.mark.skipif(not raw_data_present(), reason="data/raw/ absent")` so a fresh clone without the dataset still passes green. Spec: data-loading — "Both raw files present with the expected shape" scenario. Verify: `uv run pytest tests/test_validation.py -v` (skips cleanly without `data/raw/`; passes if present).
- [ ] 2.7 Extend `tests/test_repo_hygiene.py`'s allow-list to permit `175_341`, `82_332`, and `45` **only** inside `src/nids/validation.py`; assert those same literals (and any other integer literal `>= 10_000`) are absent from every other module under `src/nids/**` and from all of `tests/**`. Hard constraint: only `175_341`, `82_332`, `45` are permitted anywhere, and only in `validation.py`. Verify: `uv run pytest tests/test_repo_hygiene.py -v`.
- [ ] 2.8 Extend `tests/test_repo_hygiene.py` with `test_no_write_mode_open_under_raw`: AST walk over `src/nids/**` asserting no `open(..., "w"/"a"/"x")`, no `.to_csv(...)`, and no `.mkdir(...)` whose target expression derives from `raw_data_dir()`. Threat-matrix boundary: `data/raw/` read-only. Verify: `uv run pytest tests/test_repo_hygiene.py -v`.
- [ ] 2.9 Confirm every function added in this phase (`loading.py`, `validation.py`) has type hints, a docstring, and pytest coverage. Add the "Data acquisition" section content to the `README.md` placeholder from 1.6, naming both raw filenames and stating manual download is required (full README polish still deferred to Phase 5). Verify: manual review + `uv run pytest tests/test_loading.py tests/test_validation.py -v` all green.
- [ ] 2.10 **Review checkpoint.** Stop. Report the Slice 2 diff summary against the ~250-line estimate, the full test output including whether `data/raw/` is present locally (and therefore whether the real-file tests ran or skipped), and confirm `data/raw/` is byte-identical before/after the test run. Wait for the user's explicit authorization before committing `feat(data): add raw partition loading and validation` and before starting Phase 3.

---

## Phase 3: Cleaning (Slice 3)

**Delivers**: the six mandated, unfitted cleaning operations in their corrected order, every diagnostic
they emit, and the `load_clean_partitions` façade. **Reviewable against**: the step order (verified by
a fixture whose duplicate status differs under the wrong order); leakage direction (train row count
unchanged); the `removed + retained_contradictory == removed(features_only)` arithmetic identity;
`retained_contradictory == 0` under `"features_only"`; `row_count_timeline()` reading only diagnostics.
Review the step order and the leakage key before trusting any number.

- [ ] 3.1 Create `src/nids/cleaning.py` with the nine frozen dataclasses: `DropColumnsDiagnostics`, `ServiceDiagnostics`, `DeduplicationDiagnostics`, `InternalDuplicateDiagnostics`, `LeakageDiagnostics`, `LabelConflictDiagnostics`, `ClassDistributionDiagnostics`, `StepRowCounts`, and `CleaningResult` (holding `train`, `test`, and every diagnostic, plus `row_count_timeline()` which MUST read only diagnostics fields and MUST NOT reference `self.train`/`self.test`). No class defines `fit`. Spec: data-cleaning — structural groundwork for every requirement in this capability. Verify: import succeeds; structure checked by tests in 3.8–3.9.
- [ ] 3.2 Implement `drop_unused_columns(df)` (drops `DROPPED_COLUMNS`, reindexes to `KEPT_COLUMNS` for canonical column order, raises `KeyError` naming any missing column) and `normalize_service(df, *, partition)` (counts `"-"` rows before, maps via `.where(df["service"] != SERVICE_DASH, SERVICE_NONE)`, recounts `"none"` rows after on the output). Implement the shared `_row_key(df, columns)` helper (single `pd.MultiIndex.from_frame` construction point used by every later step). Steps 1–2. Spec: data-cleaning — "Fixed column drop and service normalization apply to both partitions unfitted" (both scenarios). Verify: unit tests in 3.8.
- [ ] 3.3 Implement `drop_train_duplicates(train)` (`duplicated(subset=KEPT_COLUMNS, keep="first")`, `state_counts_before/after` via `value_counts()` on input and output) and `count_test_internal_duplicates(test)` (returns only `InternalDuplicateDiagnostics`, no frame). Steps 3–4. Spec: data-cleaning — "Train-only exact-duplicate removal" (both scenarios) and "Test-internal duplicate diagnostic never removes rows" scenario. Verify: unit tests in 3.8.
- [ ] 3.4 Implement `drop_test_leakage(train, test, *, comparison_key="full_row")` using the one-pass `MultiIndex.isin` computation from design Decision 4: `removed_mask` from `leakage_key_columns(comparison_key)`, `feature_mask` on `test_clean` from `FEATURE_COLUMNS` to derive `retained_contradictory` in the same pass. Returns only the cleaned test frame plus `LeakageDiagnostics` (`comparison_key`, `rows_before`, `rows_after`, `removed`, `retained_contradictory`, `retained_contradictory_share`) — never a train frame, so the direction-critical step cannot mutate train even through a mistaken tuple unpack. Step 5. Spec: data-cleaning — "Parameterized cross-partition leakage removal, test-only and direction-safe" (all 5 scenarios). Verify: unit tests in 3.8, including the cross-key arithmetic identity.
- [ ] 3.5 Implement `count_label_conflicts(train)` using the single integer-groupby approach from the design: `pd.factorize(_row_key(train, FEATURE_COLUMNS))` → `DataFrame({"g": codes, "attack_cat": ...}).drop_duplicates()` → `groupby("g", sort=False).size()` → count groups with size `> 1`. Returns only `LabelConflictDiagnostics`, no frame. Step 6. Spec: data-cleaning — "Label-noise diagnostic on the training partition" scenario. Verify: unit tests in 3.8.
- [ ] 3.6 Implement `clean_partitions(train_raw, test_raw, *, comparison_key="full_row") -> CleaningResult`: the single composition point running steps 1–6 in exactly the mandated order, capturing `class_distribution.train_before/test_before` before step 1 and `.train_after/test_after` after step 6, with the terminal `train3.reset_index(drop=True)` / `test3.reset_index(drop=True)` that establishes deterministic positional order for downstream subsampling. **`cleaning.py` exposes no `fit` anywhere** (hard constraint). Spec: data-cleaning — "Mandated cleaning step order" (both scenarios), "No fit or fit_transform anywhere in this capability" scenario, "Cleaning output precedes any fitted preprocessing step" scenario. Verify: unit tests in 3.8; AST guard in 3.9.
- [ ] 3.7 Extend `tests/conftest.py` with the engineered fixtures: `raw_train`, `raw_test`, `cleaned` (built so the cleaned train partition is exactly 200 rows, with `proto` composition `tcp=150/75.0%`, `udp=46/23.0%`, `arp=2/exactly 1.0% kept`, `ospf=1/0.5% grouped`, `sctp=1/0.5% grouped`; 6 duplicate train rows all carrying `proto == "arp"` so the raw fixture shows `arp` at 3.9% while the cleaned fixture shows it at exactly 1.0%; a test-frame pair identical on the 41 kept columns but differing on `stcpb`; `service == "-"` on a known row count in both partitions; 2 duplicate pairs inside the test frame; 3 test rows matching train on features **and** label; 4 test rows matching train on features only with disagreeing `attack_cat`/`label`; a `proto == "gre"` value present only in test; 2 train feature combinations each carrying 2 distinct `attack_cat` values). Spec: supports every data-cleaning scenario and the preprocessing-pipeline raw-vs-cleaned fit scenario used later in Phase 4. Verify: `uv run pytest --fixtures | grep -E "raw_train|raw_test|^cleaned"` lists all three.
- [ ] 3.8 Create `tests/test_cleaning.py` covering: step order via the `stcpb`-differing fixture (duplicate status differs under the wrong order); post-drop output has exactly 41 columns with none of `id`/`stcpb`/`dtcpb`/`is_ftp_login` present; `"-"`→`"none"` count invariant; train row count strictly decreases across dedup, test row count unchanged by it; `count_test_internal_duplicates` returns a positive count and leaves test row count unchanged; `drop_test_leakage` leaves train row count unchanged and strictly decreases test row count under `"full_row"`; `retained_contradictory == 4` and `removed == 3` under `"full_row"` on the engineered fixture; `removed == 7` and `retained_contradictory == 0` under `"features_only"`; the identity `removed(full_row) + retained_contradictory(full_row) == removed(features_only)`; `count_label_conflicts` returns `>= 1` on the two-conflict fixture and leaves train unchanged; `row_count_timeline()` returns correct values when `CleaningResult` is built with empty frames (proves no second pass over `.train`/`.test`); output index is a clean `RangeIndex`; two independent runs over identical inputs reproduce identical counts. Spec: every data-cleaning requirement's scenarios. Verify: `uv run pytest tests/test_cleaning.py -v`.
- [ ] 3.9 Create `tests/test_leak_safety.py` (cleaning half): AST walk asserting `cleaning.py` defines no `FunctionDef` named `fit` or `fit_transform`, and contains no `Attribute` call expression named `fit` or `fit_transform` anywhere. Hard constraint: `cleaning.py` exposes no `fit`. Spec: data-cleaning — "The cleaning surface exposes no fit interface" scenario. Verify: `uv run pytest tests/test_leak_safety.py -v`.
- [ ] 3.10 Extend `tests/test_repo_hygiene.py`: assert no integer literal `>= 10_000` representing a cleaned-partition row count exists in `cleaning.py`, `data.py`, or `test_cleaning.py` (the existing allow-list stays scoped to `validation.py`'s three literals only). Hard constraint: no hardcoded cleaned-partition counts anywhere. Spec: data-cleaning — "No hardcoded cleaned-partition row counts" (both scenarios). Verify: `uv run pytest tests/test_repo_hygiene.py -v`.
- [ ] 3.11 Create `src/nids/data.py` (**D3**, resolved yes): `load_clean_partitions(*, comparison_key="full_row", raw_dir=None) -> CleaningResult`, composing `load_raw_train(raw_dir=raw_dir)`, `load_raw_test(raw_dir=raw_dir)`, and `clean_partitions(..., comparison_key=comparison_key)`. The only module that both reads files and cleans, keeping `cleaning.py` pure. Spec: proposal success narrative (`from nids.data import load_clean_partitions`); design Decision 11. Verify: unit test in 3.12.
- [ ] 3.12 Create `tests/test_data.py`: `load_clean_partitions()` on a monkeypatched `raw_dir` returns a `CleaningResult` equivalent to manually composing `load_raw_train` + `load_raw_test` + `clean_partitions` on the same synthetic CSVs; `comparison_key` is forwarded correctly to `clean_partitions`. Spec: same as 3.11. Verify: `uv run pytest tests/test_data.py -v`.
- [ ] 3.13 **Review checkpoint.** Stop. Report the Slice 3 diff summary against the ~360-line estimate and the full test output for `uv run pytest tests/test_cleaning.py tests/test_data.py tests/test_leak_safety.py tests/test_repo_hygiene.py -v`, calling out the step-order and leakage-key evidence explicitly. Wait for the user's explicit authorization before committing `feat(cleaning): add ordered dataset cleaning and diagnostics` and before starting Phase 4.

---

## Phase 4: Preprocessing (Slice 4)

**Delivers**: every fitted parameter in the project — `RareCategoryGrouper`, the `ColumnTransformer`/
`Pipeline` factory, and the stratified subsample helper. **This is the compliance-critical slice: every
fitted parameter in the project lives here.** **Reviewable against**: the 1% boundary at exactly 2/200
rows; the raw-vs-cleaned fit difference; unseen values mapping to `"other"`; no NaN in the design
matrix (the `log1p`-before-scale ordering proof); the TTL pair behavior; subsample allocation
determinism and the `floor=0` proportional equivalence.

- [ ] 4.1 Create `src/nids/transformers.py`: `RareCategoryGrouper(BaseEstimator, TransformerMixin)` with `__init__(threshold=RARE_CATEGORY_THRESHOLD, other_label=OTHER_CATEGORY)`, fitted attributes `frequent_categories_`, `category_counts_`, `n_rows_fit_`, `feature_names_in_`, `n_features_in_`. `fit(X, y=None)`: `TypeError` naming the received type when `X` is not a `pd.DataFrame`; `ValueError` when `threshold` is outside `[0.0, 1.0)`; `ValueError` on an empty frame; per column, `frequent_categories_[col] = frozenset(counts.index[counts / n_rows_fit_ >= threshold])` (**`>=` keeps — boundary is inclusive**); `ValueError` if `other_label` already appears among fitted categories. `transform(X)`: `check_is_fitted` first; `ValueError` if columns don't match `feature_names_in_`; both rare-in-train and unseen-in-train values map to `other_label` via the identical code path (no `if unseen` branch); output cast with `.astype(STRING_DTYPE)`; never mutates `X`. `get_feature_names_out(input_features=None)` implemented without the private `sklearn.utils.validation._check_feature_names_in` helper. Spec: preprocessing-pipeline — "Rare-category grouping is fitted state, never a hardcoded list" (all 3 scenarios), "The rare-category threshold is fitted on the cleaned training partition only" scenario. Verify: unit tests in 4.4.
- [ ] 4.2 Create `src/nids/preprocessing.py`: `build_preprocessor(include_ttl_features: bool = True) -> Pipeline` and `build_preprocessor_pair() -> dict[str, Pipeline]`. **No function in this module takes a DataFrame or any data parameter — the entire public surface is one `bool` and one no-argument function** (hard constraint / Decision 5). Assemble the `ColumnTransformer` exactly per design: `categorical` branch = `RareCategoryGrouper()` → `OneHotEncoder(handle_unknown="ignore", sparse_output=False, dtype=np.float64)` over `RARE_GROUPED_COLUMNS`; `skewed` branch = `FunctionTransformer(np.log1p, inverse_func=np.expm1, feature_names_out="one-to-one", validate=False)` → `StandardScaler()` over `skewed_feature_columns(include_ttl_features)`; `numeric` branch = `StandardScaler()` over `numeric_feature_columns(include_ttl_features)`; `remainder="drop"`, `verbose_feature_names_out=False`. Add a runtime assertion that the three column groups are pairwise disjoint and exhaustive over the selected features. Wrap as `Pipeline([("features", column_transformer)])`. Spec: preprocessing-pipeline — "One-hot encoding is fitted on the training partition only" scenario, "Skewed volumetric features are log-transformed then scaled, scaler fitted on train only" (both scenarios), "The TTL shortcut inclusion toggle" (all 3 scenarios), "No fitted component of this capability accepts a test partition for fitting" scenario. Verify: unit/integration tests in 4.5.
- [ ] 4.3 Create `src/nids/sampling.py`: `ClassAllocation` (`label`, `available`, `allocated`, `proportional`, `floor_applied`, `population_share`, `sample_share`), `SubsampleResult` (`data`, `allocations`, `stratify_by`, `requested_max_rows`, `floor`, `seed`, `total_rows`, `floored_classes` property, `to_frame()`), `stratified_subsample(df, *, stratify_by="attack_cat", max_rows=10_000, floor=50, seed=42)`. Implement the largest-remainder allocation exactly per design step table: guards (`ValueError`/`KeyError` on bad `max_rows`/`floor`/empty `df`/missing `stratify_by` column); **an input already at or below the cap returns unmodified with `floor_applied=False` for every class, consuming no RNG** (user-approved edge case); floor feasibility check raising `ValueError` naming `floor`, the offending class count, and `max_rows`; proportional distribution of the remainder by `floor(exact_c)`; residual distribution by largest fractional remainder, ties broken by class label ascending, no randomness; reference allocation with `floor=0` to derive `proportional_c` and set `floor_applied = allocated > proportional`; **a class with fewer rows than the floor returns every available row for that class with `floor_applied=True`** (user-approved edge case); one `np.random.default_rng(seed)` created once, classes iterated in sorted label order, `rng.choice(positions, size=a_c, replace=False)` per class; output positions concatenated and sorted ascending before `.iloc[...].reset_index(drop=True)`. Spec: stratified-subsampling — every requirement's scenarios (deterministic bounded subsample, minimum-per-class floor, class-smaller-than-floor, input-at-or-below-cap, deterministic-ordering precondition). Verify: unit tests in 4.6.
- [ ] 4.4 Create `tests/test_transformers.py`: boundary at exactly 1% (2/200 fixture) is kept; 0.5% (1/200) is grouped into `"other"`; a value unseen in `fit` maps to `"other"`; grouper fitted on frame A and applied to frame B yields frame A's category set (not frame B's); `clone()`/`get_params`/`set_params` round-trip; `fit_transform(X) == fit(X).transform(X)`; `ValueError` when `other_label` collides with a real fitted category; `TypeError` on ndarray input naming the received type; **raw-vs-cleaned fit produces different `frequent_categories_`** using the engineered `arp` fixture (8/206 = 3.9% on raw, kept; exactly 2/200 = 1.0% on cleaned, kept only at the boundary — demonstrating the real-data 128-vs-130 finding in miniature). Spec: preprocessing-pipeline — "Rare-category grouping is fitted state" (all 3 scenarios) and "threshold fitted on cleaned training partition only" scenario. Verify: `uv run pytest tests/test_transformers.py -v`.
- [ ] 4.5 Create `tests/test_preprocessing.py`: `fit` on `cleaned.train[feature_columns()]`, `transform` on `cleaned.test[feature_columns()]` — shapes agree; `get_feature_names_out()` excludes both `attack_cat` and `label` (belt-and-braces via `remainder="drop"` even when the full 41-column frame is passed); `include_ttl_features=False` output excludes `sttl`/`ct_state_ttl` with exactly two fewer columns than the default; no `NaN` anywhere in the transformed output (proves the `log1p`-before-scale ordering); `pipe.set_output(transform="pandas")` yields the expected readable column names (`proto_tcp`, `sbytes`, not `categorical__proto_tcp`); the test-only `proto == "gre"` fixture value produces `proto_other == 1`, not an all-zero one-hot block; `build_preprocessor_pair()` returns `{"with_ttl": ..., "without_ttl": ...}` differing only in the TTL columns, with every other fitted parameter computed independently and correctly for each. Spec: preprocessing-pipeline — "One-hot encoding fitted on training only" scenario, "Skewed features" (both scenarios), "TTL toggle" (all 3 scenarios), "feature set explicit allow-list" (both scenarios, integration confirmation), "No fitted component accepts a test partition for fitting" scenario. Verify: `uv run pytest tests/test_preprocessing.py -v`.
- [ ] 4.6 Create `tests/test_sampling.py`: two calls with seed 42 on the same input return identical row content; total selected rows equals `max_rows` exactly; a class smaller than `floor` yields all its available rows with `floor_applied=True`; `floor=0` matches an independently computed proportional largest-remainder allocation with `floor_applied=False` for every class; `N <= max_rows` returns the input unchanged with every `floor_applied=False` and no RNG consumption; an infeasible floor (`sum(min(n_c, floor)) > max_rows`) raises `ValueError` naming `floor` and `max_rows`; a 1,000-row, 10-class fixture with a 3-row class and a 700-row class, `max_rows=100, floor=5`, lands exactly on 100 total; deterministic-ordering test — two content-equal frames with different underlying row order, each index-reset before the call, select the same underlying rows. Spec: stratified-subsampling — every requirement's scenarios. Verify: `uv run pytest tests/test_sampling.py -v`.
- [ ] 4.7 Extend `tests/test_leak_safety.py` (preprocessing half): AST walk asserting every public function's parameters in `preprocessing.py` are annotated `bool` or take no parameters at all — no parameter named `X`, `test`, `X_test`, `df`, or `test_df`, and no parameter annotated as a DataFrame type. Hard constraint: `preprocessing.py` accepts no DataFrame parameter. Spec: preprocessing-pipeline — "No fitted component of this capability accepts a test partition for fitting" — "The preprocessing surface exposes no test-fitting path" scenario. Verify: `uv run pytest tests/test_leak_safety.py -v`.
- [ ] 4.8 Extend `tests/test_repo_hygiene.py`'s allow-list to permit `10_000` only inside `src/nids/sampling.py`; re-run the full-repo literal scan across every module created through this slice and confirm no other `>= 10_000` literal exists. Hard constraint. Verify: `uv run pytest tests/test_repo_hygiene.py -v`.
- [ ] 4.9 **Review checkpoint.** Stop. Report the Slice 4 diff summary against the ~380-line estimate and the full test output for `uv run pytest tests/test_transformers.py tests/test_preprocessing.py tests/test_sampling.py tests/test_leak_safety.py tests/test_repo_hygiene.py -v`. Flag this slice as the compliance-critical one for review priority (every fitted parameter in the project lives here). Wait for the user's explicit authorization before committing `feat(preprocessing): add fitted pipeline factory and subsampling` and before starting Phase 5.

---

## Phase 5: Results (Slice 5)

**Delivers**: the `results/<notebook_id>/` output-contract writer, the cleaning report as its first
producer, and the completed README. **Reviewable against**: manifest round-trip and every declared
path existing on disk; the manifest written last; a `SOURCE_DATE_EPOCH` rerun producing a
byte-identical tree; `counts.json` keys matching the manifest's metric names; both mandatory notes
present; `prune_undeclared` removing only orphaned files.

- [ ] 5.1 Create `src/nids/results.py`: `ArtifactEntry`, `MetricEntry`, `NoteEntry` (**D2 resolved — the additive `notes` array**), `ResultsWriter.__init__(notebook_id, *, root=None, generated_at=None, prune_undeclared=True)`, `directory` property, `add_table(frame, *, name, title, description, sort_by=None)`, `add_json_table(payload, *, name, title, description)`, `add_figure(figure, *, name, title, description)`, `add_metric(name, value, description)`, `add_note(note_id, text)`, `write_json(payload, *, name)`, `write_manifest()`, `close()`, `__enter__`/`__exit__`. Implement every design point: `notebook_id`/artifact name validated against `^[a-z][a-z0-9_]{0,63}$` before any filesystem call; duplicate `name` raises `ValueError`; every artifact written to `<path>.tmp` then `os.replace`d; **manifest written last**, stats every declared relative path first and raises before writing if one is missing; manifest paths built with `PurePosixPath`; deterministic CSV (`to_csv(index=False, lineterminator="\n", float_format="%.6f", encoding="utf-8")`, optional `sort_by` with `kind="stable"`); deterministic JSON (`json.dumps(payload, indent=2, ensure_ascii=False, sort_keys=False)` plus trailing newline, `newline="\n"`); deterministic PNG (`savefig(..., metadata={"Software": None, "Creation Time": None})`); entries emitted in registration order; `prune_undeclared=True` default removes only orphaned registered-extension files directly under this notebook's own `tables/`/`figures/`, reported by `close()`; `generated_at` resolution order constructor arg → `SOURCE_DATE_EPOCH` env var → `datetime.now(UTC)`, formatted `%Y-%m-%dT%H:%M:%SZ`. Spec: results-output-contract — "Self-describing per-producer output folder layout" (both scenarios), "One manifest.json per folder with the required schema" (all 3 scenarios), "Deterministic, timestamp-free file naming" scenario, "Reruns over identical inputs are reproducible at the byte level for table files" scenario, "results/ is committed while data/raw/ stays untouched by this contract" (both scenarios). Verify: unit tests in 5.4.
- [ ] 5.2 Create `src/nids/cleaning_report.py`: `NOTEBOOK_ID = "data_cleaning"`, `build_cleaning_report(*, comparison_key="full_row", raw_dir=None, results_root=None, generated_at=None) -> Path` (runs `validation.validate_raw_data()` first and returns/exits with download instructions on failure — `cleaning_report` depends on `validation`, never the reverse), `main(argv=None) -> int` (argparse `--comparison-key {full_row,features_only}`). Writes `tables/row_counts_before_after.csv` (from `CleaningResult.row_count_timeline()`, execution order, not re-sorted), `tables/attack_cat_distribution.csv` (sort `train_rows_after` desc then `attack_cat` asc), `tables/state_distribution.csv` (sort `rows_before` desc then `state` asc), `tables/rare_categories.csv` (from a `RareCategoryGrouper` fitted on `result.train[RARE_GROUPED_COLUMNS]`, sort `column` asc, `rows` desc, `category` asc), `figures/class_balance_before_after.png` (grouped bars, log y-scale, English title and axis labels), `counts.json` (`{e.name: e.value for e in metrics}` plus `notes`, emitted from the same `MetricEntry` list as the manifest so the two cannot drift), and the twelve named metrics (`train_duplicate_rows_dropped`, `test_internal_duplicate_rows`, `test_leakage_rows_dropped`, `test_leakage_comparison_key`, `test_contradictory_rows_retained`, `test_contradictory_share_of_retained`, `label_noise_floor_combinations`, `label_noise_floor_rows`, `service_dash_rows_mapped`, `raw_null_cells`, `train_rows_final`, `test_rows_final`) plus both notes (`benchmark_non_comparability`, `retained_error_floor`). **This module contains no `len()`, `.shape`, `value_counts()`, `duplicated()`, or `groupby()` applied to a partition frame anywhere** (hard constraint / Decision 2) — every number comes from `CleaningResult` or `RareCategoryGrouper.category_counts_`. Spec: results-output-contract — "The cleaning report is this contract's first producer" (all 4 scenarios). Verify: unit/integration tests in 5.5.
- [ ] 5.3 Complete `README.md`: `## Setup` (`uv sync`), `## Data acquisition` (manual UNSW-NB15 download naming both expected filenames — no automated download exists), `## Running notebooks` (`uv run jupyter lab` from the repository root, and why a separately registered kernel would not see the package), `## Validation and reports` (`uv run python -m nids.validation`, `uv run python -m nids.cleaning_report`), `## Known limitations` (the benchmark non-comparability note, the retained error-floor note, the subsample floor distortion — Worms over-represented roughly 4x against its true population share). Spec: proposal deliverable 8; project-packaging — "notebook kernel" documentation scenarios; results-output-contract — both mandatory notes. Verify: manual read-through against the proposal's README requirements; no absolute paths present.
- [ ] 5.4 Create `tests/test_results.py`: manifest round-trips through `json.load`; every declared `tables`/`figures` path exists relative to the manifest's own folder; registering the same `name` twice raises `ValueError`; traversal/invalid names (`..`, `/`, a leading `/`, an extension, `Bad-Id`) are rejected before any filesystem call is made; manifest is written last (simulate a mid-run exception after `add_table`/`add_figure`, assert `manifest.json` does not exist); two runs with the same fixed `generated_at` (via `frozen_clock`) produce byte-identical files; `prune_undeclared` removes only orphaned registered-extension files directly under this notebook's `tables/`/`figures/` and reports each pruned path; PNG bytes carry no `Software`/`Creation Time` text chunk after the metadata override. Spec: results-output-contract — "manifest round-trips through JSON parsing with all declared paths present" scenario, "Consumers discover outputs without a global index" scenario, "File names carry no timestamp" scenario, "Two consecutive runs produce identical table bytes" scenario. Verify: `uv run pytest tests/test_results.py -v`.
- [ ] 5.5 Create `tests/test_cleaning_report.py`: `build_cleaning_report` against synthetic partitions with a monkeypatched `raw_dir` and `results_root=tmp_results_root`, using the `Agg` matplotlib backend, produces `manifest.json` + `counts.json` + the 4 required tables + the 1 required figure; `counts.json` keys equal the manifest's metric names exactly; both mandatory notes are present in the manifest's `notes` array; a second run with the same fixed `generated_at` is byte-identical to the first; the leakage comparison key, `test_leakage_rows_dropped`, and `test_contradictory_rows_retained` (with its share) all appear together in `metrics`; perturbing the synthetic fixture changes the reported metric values accordingly (proving they are computed, not hardcoded). Add `test_end_to_end_on_real_partitions`, marked `@pytest.mark.slow` **and** `@pytest.mark.skipif(not raw_data_present(), reason="data/raw/ absent")` — runs the whole report against real `data/raw/` (read-only) and asserts the arithmetic identity holds on real data; excluded from the default run by `addopts = ["-m", "not slow"]`. Spec: results-output-contract — "The cleaning report is this contract's first producer" (all 4 scenarios). Verify: `uv run pytest tests/test_cleaning_report.py -v`; optionally `uv run pytest tests/test_cleaning_report.py -m slow` when `data/raw/` is present.
- [ ] 5.6 Extend `tests/test_repo_hygiene.py` with the `cleaning_report.py` no-recompute rule: AST walk asserting `cleaning_report.py` contains no `len`, `value_counts`, `duplicated`, `groupby`, or `.shape` applied to a frame anywhere in the module. Hard constraint / Decision 2. Verify: `uv run pytest tests/test_repo_hygiene.py -v`.
- [ ] 5.7 If `data/raw/` (read-only) is present locally, run `uv run python -m nids.cleaning_report` to generate and commit `results/data_cleaning/**` (`manifest.json`, `counts.json`, the four tables, the one figure) as this change's only generated-artifact content. If `data/raw/` is absent, mark this task explicitly blocked pending manual data acquisition and do not fabricate output. Spec: results-output-contract — "A producer's output is available without rerunning it" scenario; proposal deliverable 6. Verify: `results/data_cleaning/manifest.json` parses via `json.load` and every declared path exists; `data/raw/` (read-only) is byte-identical before and after the run.
- [ ] 5.8 Full-suite verification: `uv run pytest` passes with `data/raw/` present; separately, with `raw_dir` pointed at an empty temp directory via monkeypatch or `NIDS_REPO_ROOT`/fixture override (never by moving or renaming the real `data/raw/` (read-only)), confirm every `skipif`-guarded real-file test skips cleanly and the full suite still passes; `uv sync && uv run python -c "import nids"` succeeds; re-run `test_repo_hygiene.py` and `test_leak_safety.py` one final time across the complete `src/nids/**` tree. Spec: proposal success criteria — "Packaging and environment" section. Verify: recorded `pytest` output for both states of `data/raw/` availability.
- [ ] 5.9 **Review checkpoint.** Stop. Report the Slice 5 diff summary against the ~340-line estimate, the full test output for `uv run pytest tests/test_results.py tests/test_cleaning_report.py tests/test_repo_hygiene.py -v`, and the full-change verification results from 5.8. Wait for the user's explicit authorization before committing `feat(results): add output contract and cleaning report` — the final of the five slices.

---

## Acceptance checklist (cross-cutting, verified across all five review checkpoints)

- [ ] No hardcoded cleaned-partition counts anywhere in `src/nids` or `tests`; only `175_341`, `82_332`, and `45` are permitted, and only in `validation.py` — enforced incrementally by `tests/test_repo_hygiene.py` in Phases 1, 2, 3, 4.
- [ ] `cleaning.py` exposes no `fit` — enforced by `tests/test_leak_safety.py` in Phase 3.
- [ ] `preprocessing.py` accepts no DataFrame parameter — enforced by `tests/test_leak_safety.py` in Phase 4.
- [ ] Nothing writes to `data/raw/` (read-only) — enforced by `tests/test_repo_hygiene.py::test_no_write_mode_open_under_raw` in Phase 2, and reverified in Phase 5.
- [ ] No automated dataset download anywhere — `validation.main()` only prints instructions and exits non-zero; verified in Phase 2.
- [ ] `feature_columns()` is an explicit allow-list; `attack_cat`/`label` never reach the feature matrix — verified in Phase 1 (`columns.py`) and Phase 4 (`preprocessing.py` integration).
- [ ] `leakage_key_columns()` never takes the TTL toggle — verified in Phase 1.
- [ ] Determinism: reruns of the cleaning report produce identical computed counts and byte-identical table files — verified in Phase 5.
- [ ] Every function in `src/nids` has type hints, a docstring, and pytest tests — verified per-phase at each module-creation task and at each review checkpoint.
